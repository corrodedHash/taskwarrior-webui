export default defineEventHandler(async (event) => {
    console.log(event.context.params);
    console.log(event.node.req.method)
    console.log(event.node.req.)
    const a = await $fetch.raw("http://localhost:3000/tasks", {
      method: "get",
    });
    return a._data
  });
  